require 'spec_helper'

describe SfFilmLocationApiWrapper do
  it 'has a version number' do
    expect(SfFilmLocationApiWrapper::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
